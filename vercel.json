{
  "installCommand": "cd site && npm install -g pnpm@10 && pnpm install --force",
  "buildCommand": "cd site && pnpm run build",
  "outputDirectory": "site/dist",
  "rewrites": [
    {
      "source": "/u/:id",
      "destination": "/site/api/index?id=:id"
    }
  ]
}
